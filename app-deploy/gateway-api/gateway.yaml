apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-tls-cert
  namespace: dev
spec:
  secretName: app-tls-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - utsabsapkota.xyz
    - argocd.utsabsapkota.xyz
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: my-gateway
  namespace: dev
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  gatewayClassName: nginx
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: "utsabsapkota.xyz"
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      hostname: "utsabsapkota.xyz"
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: app-tls-cert
      allowedRoutes:
        namespaces:
          from: All
    - name: argo-http
      port: 80
      protocol: HTTP
      hostname: "argocd.utsabsapkota.xyz"
    - name: argo-https
      hostname: "argocd.utsabsapkota.xyz"
      port: 443
      protocol: HTTPS
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
        - name: app-tls-cert
          kind: Secret
          group: ""
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backend-route
  namespace: dev
spec:
  parentRefs:
    - name: my-gateway
      namespace: dev
  hostnames:
  - "utsabsapkota.xyz"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api
      backendRefs:
        - name: backend-service
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: frontend-service
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: dev
spec:
  parentRefs:
    - name: my-gateway
      namespace: dev
  hostnames:
    - "argocd.utsabsapkota.xyz"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: argocd-server
          namespace: argocd
          port: 80
